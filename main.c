
#include "lem-in.h"

void	setting_links(char *start, char *end, char *list)
{
		this will redo the number of **
		paste everything in and append the links on start and end node
}

int		asses_line(char *str, t_room *start)
{
	if () setting start 
	if () setting end
	if () setting points
	if () setting links

}

int		main(void)
{
	char	*line;
	t_room	*start;

	while (get_next_line(1, line) > 0)
	{
		if (!asses_line(line, start))
			return (free_everything);

	}
	return (0);
}